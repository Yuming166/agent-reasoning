# Data metadata

This directory contains small provenance records, checksums, and the 27,613-row
EX-Graph address mapping used to filter the BigQuery event sources. Raw and
large files referenced by the manifests are intentionally not checked in.
